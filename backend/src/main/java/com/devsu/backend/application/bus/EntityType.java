package com.devsu.backend.application.bus;

/**
 * Enum EntityType representing different entity categories in the system.
 */
public enum EntityType {
    CLIENT,
    ACCOUNT,
    MOVEMENT,
    REPORT
}